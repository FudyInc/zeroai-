__pycache__/
*.pyc
.env
.env.bak-*
google-sheets-key.json
state.json
crm.json
inbox.json
finance.json
users.json
activity.json
# telemetría de agentes (anillo local, se regenera solo)
agent_activity.json
# cola de trabajo autónomo (local, coordina procesos de esta máquina)
tareas.json
tareas.json.tmp
*.json.bak
*.json.bak-*
*.json.tmp
.venv/
venv/

# frontend
frontend/node_modules/
frontend/dist/
node_modules/

# otros
*.mp3
.DS_Store

# obsidian (config local del editor)
.obsidian/

# Claude Code — overrides personales, no compartidos
.claude/settings.local.json

# informe de la auditoría diaria (se regenera; no es fuente de verdad)
auditoria.json
# su hermano de criterio: lo escribe AUDIT a mano desde otra rama y el planificador lo
# consume. Tampoco es fuente de verdad —el historial de salud vive en audit/diaria— y
# versionarlo haría que cada corrida dejara el repo sucio. El patrón cubre las variantes.
auditoria-criterio*.json
